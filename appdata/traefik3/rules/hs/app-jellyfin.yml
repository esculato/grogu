http:
  routers:
    jellyfin-rtr:
      rule: "Host(`jellyfin.{{env "DOMAINNAME"}}`)"
      entryPoints:
        - websecure-internal
        - websecure-external
      middlewares:
        - chain-no-auth@file
      service: jellyin-svc
      tls:
        certResolver: dns-cloudflare
  services:
    jellyfin-svc:
      loadBalancer:
        servers:
          - url: "http://192.168.5.7:8036
