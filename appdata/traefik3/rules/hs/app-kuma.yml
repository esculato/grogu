http:
  routers:
    kuma-rtr:
      rule: "Host(`kuma.{{env "DOMAINNAME"}}`)"
      entryPoints:
        - websecure-internal
        - websecure-external
      middlewares:
        - chain-authelia@file
      service: kuma-svc
      tls:
        certResolver: dns-cloudflare
  services:
    kuma-svc:
      loadBalancer:
        servers:
          - url: "http://kuma:3001"
